/**:
  ros__parameters:
    # 3.0 m — identical to the working full stack (Electrans_project), which on
    # this exact robot+map converges in 1 NDT iteration (NVTL 2.91) from ~123
    # downsampled points. Verified live: point count was never the problem; my
    # earlier 0.5/1.0 experiments were chasing a lag that was actually the
    # thrashing concat. Keep this matched to Electrans.
    voxel_size_x: 3.0
    voxel_size_y: 3.0
    voxel_size_z: 3.0
