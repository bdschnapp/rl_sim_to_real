<launch>
  <arg name="launch_driver" default="true"/>
  <arg name="host_ip" default="192.168.1.102"/>
  <!-- Single-lidar robot: the concatenate node has nothing to concatenate. With
       the >=2-topic workaround it thrashed (constant "Reset oldest collector" +
       "transformed_raw_points nullptr"), burning a CPU core in the sensing
       container and lagging the WHOLE chain ~2.7 s. That made every NDT pose
       arrive past the EKF's ~1 s compensation limit ("Pose delay exceeds ...
       ignored"), so localization never corrected. Disabled: NDT reads
       /sensing/lidar/front/pointcloud_before_sync directly (see
       tier4_localization_component input_pointcloud); only RViz consumed the
       concatenated topic. Re-enable + fix the matching_strategy if a 2nd lidar
       is ever added. -->
  <arg name="use_concat_filter" default="False"/>
  <arg name="vehicle_id" default="$(env VEHICLE_ID default)"/>
  <arg name="vehicle_mirror_param_file"/>
  <arg name="pointcloud_container_name" default="pointcloud_container"/>

  <group>
    <push-ros-namespace namespace="lidar"/>

    <group>
      <push-ros-namespace namespace="front"/>
      <include file="$(find-pkg-share electrans_robot_sensor_launch)/launch/robosense_Helios.launch.xml">
        <arg name="launch_driver" value="$(var launch_driver)"/>
        <arg name="max_range" value="150.0"/>
        <arg name="frame_id" value="rslidar_front"/>
        <arg name="output_topic" value="/rslidar_points_front"/>
        <arg name="return_mode" value="Strongest"/>
        <arg name="sensor_ip" value="192.168.1.200"/>
        <arg name="host_ip" value="$(var host_ip)"/>
        <arg name="data_port" value="6699"/>
        <arg name="scan_phase" value="300.0"/>
        <arg name="vehicle_mirror_param_file" value="$(var vehicle_mirror_param_file)"/>
        <arg name="container_name" value="pointcloud_container"/>
      </include>
    </group>

  

    <include file="$(find-pkg-share electrans_robot_sensor_kit_launch)/launch/pointcloud_preprocessor.launch.py">
      <arg name="base_frame" value="base_link"/>
      <arg name="use_intra_process" value="true"/>
      <arg name="use_multithread" value="true"/>
      <arg name="pointcloud_container_name" value="$(var pointcloud_container_name)"/>
    </include>
  </group>
</launch>
