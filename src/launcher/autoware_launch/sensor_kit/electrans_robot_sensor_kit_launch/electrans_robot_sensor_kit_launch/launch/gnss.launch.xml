<launch>
  <!-- Driver OFF by default (2026-07-01): the lab robot runs indoors with no
       GNSS fix ever used (pose_twist_estimator gnss_enabled=false), and the
       u-blox serial device wedges the node into an error flood
       ("U-Blox ASIO input buffer read error: End of file") that bloats logs
       and burns CPU. The gnss_poser below stays (harmless without input) so
       the topic graph is unchanged. Set launch_gnss_driver:=true if a real
       receiver is ever connected. -->
  <arg name="launch_driver" default="true"/>
  <arg name="launch_gnss_driver" default="false"/>
  <let name="gnss_driver_on" value="$(eval &quot;'$(var launch_driver)'=='true' and '$(var launch_gnss_driver)'=='true'&quot;)"/>
  <arg name="gnss_receiver" default="ublox" description="ublox(default) or septentrio"/>

  <group>
    <push-ros-namespace namespace="gnss"/>

    <!-- Switch topic name -->
    <let name="navsatfix_topic_name" value="ublox/nav_sat_fix" if="$(eval &quot;'$(var gnss_receiver)'=='ublox'&quot;)"/>
    <let name="navsatfix_topic_name" value="septentrio/nav_sat_fix" if="$(eval &quot;'$(var gnss_receiver)'=='septentrio'&quot;)"/>
    <let name="orientation_topic_name" value="/autoware_orientation"/>

    <!-- Ublox Driver -->
    <group if="$(eval &quot;'$(var gnss_receiver)'=='ublox'&quot;)">
      <node pkg="ublox_gps" name="ublox" exec="ublox_gps_node" if="$(var gnss_driver_on)">
        <remap from="~/fix" to="~/nav_sat_fix"/>
        <param from="$(find-pkg-share ublox_gps)/config/zed_f9p.yaml"/>
        <!-- zed_f9p.yaml is scoped under 'ublox_gps_node:' but this node is named 'ublox',
             so the YAML params don't match. Explicitly set tmode3 to ensure the HPG
             ref product driver doesn't throw "TMODE3 must be set". -->
        <param name="tmode3" value="0"/>
      </node>
    </group>

    <!-- Septentrio GNSS Driver -->
    <!-- <group if="$(eval &quot;'$(var launch_driver)' and '$(var gnss_receiver)'=='septentrio'&quot;)">
      <include file="$(find-pkg-share septentrio_gnss_driver)/launch/mosaic_x5_rover.launch.xml"/>
    </group> -->

    <!-- NavSatFix to MGRS Pose -->
    <include file="$(find-pkg-share autoware_gnss_poser)/launch/gnss_poser.launch.xml">
      <arg name="input_topic_fix" value="$(var navsatfix_topic_name)"/>
      <arg name="input_topic_orientation" value="$(var orientation_topic_name)"/>

      <arg name="output_topic_gnss_pose" value="pose"/>
      <arg name="output_topic_gnss_pose_cov" value="pose_with_covariance"/>
      <arg name="output_topic_gnss_fixed" value="fixed"/>

      <arg name="use_gnss_ins_orientation" value="true"/>
    </include>
  </group>
</launch>
